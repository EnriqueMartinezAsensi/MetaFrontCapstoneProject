import styled from "styled-components";

export const CounterContainer = styled.div`
  display: flex;
  margin: 0rem;
  width: min-content;
  height: min-content;
`
export const CounterElement = styled.p`
  padding: 0rem 0.4rem;
  margin: 0;
  &:hover{
    cursor: pointer;
  }
`